import { ProfileLayout, ProfileModule } from 'containers/modules';

const ProfilePage = () => {
  return (
    <ProfileLayout>
      <ProfileModule />
    </ProfileLayout>
  );
};

export default ProfilePage;
